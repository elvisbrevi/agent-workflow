Issue #281 -- una lectura de terminal cuyo plazo vence ya no compite con la
ronda siguiente; la lectura compartida conserva la respuesta posterior.

== bun test (focused) ==
$ (cd agent/lazy-workflow && bun test test/question-channel.test.ts)

bun test v1.3.14

16 pass
0 fail
30 expect() calls
Ran 16 tests across 1 file. [47.00ms]

== bun test (full suite) ==
$ (cd agent/lazy-workflow && bun test)

907 pass
0 fail
3093 expect() calls
Ran 907 tests across 46 files. [12.16s]

== installer suite ==
$ bash tests/install_test.sh

PASS: no-TTY invocation reports an actionable error
PASS: piped invocation accepts an explicit non-interactive mode
PASS: shared global install and uninstall process every entry
PASS: Claude Code destinations install and uninstall skills, agents, and runners
PASS: unified global mode installs and uninstalls every global integration
PASS: install refreshes dirty cache and reconciles only repository-owned links
6 installer tests passed.

== diff check ==
$ git diff --check

passed with no output
